// TODO Crie um programa de contagem progressiva. Peça um número e conte de 0 até esse número, usando um loop while no console do navegador.

let = numeroMaximo = prompt('Digite um numero para a contagem regressiva');
let contador = 0;

while (numeroMaximo <= numeroMaximo) {
    console.log(contador)
    numeroMaximo++;
}